# 实践项目建议 🚀

> **一句话总结**: 实践项目建议 🚀的详细讲解与实战指南

> **难度等级**: ⭐⭐⭐ (进阶级)
> **预计学习时间**: 2-3天
> **前置知识**: 待补充
> **学习目标**:
> - 理论: 待补充
> - 实践: 待补充
> - 应用: 待补充

---


> **通过实际项目深入理解经典架构**
>
> **版本**: v1.1 | **更新**: 2026-12-23 | **难度**: ⭐⭐⭐⭐

---

## 项目1: 架构复现与对比

**目标**: 在CIFAR-10上复现所有架构并对比

**难度**: ⭐⭐⭐ | **时间**: 3-5天

### 完整代码框架

```python

# 依赖: matplotlib, numpy, torch, torchvision
# 安装: pip install matplotlib numpy torch torchvision
import torch
import torch.nn as nn
import torch.optim as optim
from torchvision import datasets, transforms
from torch.utils.data import DataLoader
import matplotlib.pyplot as plt
import numpy as np

# 1. 数据准备

transform_train = transforms.Compose([
    transforms.RandomCrop(32, padding=4),
    transforms.RandomHorizontalFlip(),
    transforms.ToTensor(),
    transforms.Normalize((0.4914, 0.4822, 0.4465), 
                        (0.2023, 0.1994, 0.2010))
])

transform_test = transforms.Compose([
    transforms.ToTensor(),
    transforms.Normalize((0.4914, 0.4822, 0.4465), 
                        (0.2023, 0.1994, 0.2010))
])

train_loader = DataLoader(
    datasets.CIFAR10('./data', train=True, download=True, 
                    transform=transform_train),
    batch_size=128, shuffle=True
)

val_loader = DataLoader(
    datasets.CIFAR10('./data', train=False, download=True, 
                    transform=transform_test),
    batch_size=128, shuffle=False
)

# 2. 模型定义（适配CIFAR-10）

class LeNet5_CIFAR(nn.Module):
    """适配CIFAR-10的LeNet-5"""
    def __init__(self, num_classes=10):
        super().__init__()
        self.features = nn.Sequential(
            nn.Conv2d(3, 6, kernel_size=5, padding=2),
            nn.AvgPool2d(kernel_size=2, stride=2),
            nn.ReLU(inplace=True),
            nn.Conv2d(6, 16, kernel_size=5),
            nn.AvgPool2d(kernel_size=2, stride=2),
            nn.ReLU(inplace=True),
        )
        self.classifier = nn.Sequential(
            nn.Linear(16 * 6 * 6, 120),
            nn.ReLU(inplace=True),
            nn.Linear(120, 84),
            nn.ReLU(inplace=True),
            nn.Linear(84, num_classes)
        )
    
    def forward(self, x):
        x = self.features(x)
        x = x.view(x.size(0), -1)
        x = self.classifier(x)
        return x

class AlexNet_CIFAR(nn.Module):
    """适配CIFAR-10的AlexNet"""
    def __init__(self, num_classes=10):
        super().__init__()
        self.features = nn.Sequential(
            nn.Conv2d(3, 64, kernel_size=3, padding=1),
            nn.ReLU(inplace=True),
            nn.MaxPool2d(kernel_size=2, stride=2),
            nn.Conv2d(64, 192, kernel_size=3, padding=1),
            nn.ReLU(inplace=True),
            nn.MaxPool2d(kernel_size=2, stride=2),
            nn.Conv2d(192, 384, kernel_size=3, padding=1),
            nn.ReLU(inplace=True),
            nn.Conv2d(384, 256, kernel_size=3, padding=1),
            nn.ReLU(inplace=True),
            nn.Conv2d(256, 256, kernel_size=3, padding=1),
            nn.ReLU(inplace=True),
            nn.MaxPool2d(kernel_size=2, stride=2),
        )
        self.classifier = nn.Sequential(
            nn.Dropout(0.5),
            nn.Linear(256 * 4 * 4, 4096),
            nn.ReLU(inplace=True),
            nn.Dropout(0.5),
            nn.Linear(4096, 4096),
            nn.ReLU(inplace=True),
            nn.Linear(4096, num_classes),
        )
    
    def forward(self, x):
        x = self.features(x)
        x = x.view(x.size(0), -1)
        x = self.classifier(x)
        return x

class VGG16_CIFAR(nn.Module):
    """适配CIFAR-10的VGG16"""
    def __init__(self, num_classes=10):
        super().__init__()
        self.features = nn.Sequential(
            nn.Conv2d(3, 64, kernel_size=3, padding=1),
            nn.ReLU(inplace=True),
            nn.Conv2d(64, 64, kernel_size=3, padding=1),
            nn.ReLU(inplace=True),
            nn.MaxPool2d(kernel_size=2, stride=2),
            
            nn.Conv2d(64, 128, kernel_size=3, padding=1),
            nn.ReLU(inplace=True),
            nn.Conv2d(128, 128, kernel_size=3, padding=1),
            nn.ReLU(inplace=True),
            nn.MaxPool2d(kernel_size=2, stride=2),
            
            nn.Conv2d(128, 256, kernel_size=3, padding=1),
            nn.ReLU(inplace=True),
            nn.Conv2d(256, 256, kernel_size=3, padding=1),
            nn.ReLU(inplace=True),
            nn.Conv2d(256, 256, kernel_size=3, padding=1),
            nn.ReLU(inplace=True),
            nn.MaxPool2d(kernel_size=2, stride=2),
        )
        self.classifier = nn.Sequential(
            nn.Linear(256 * 4 * 4, 4096),
            nn.ReLU(inplace=True),
            nn.Dropout(0.5),
            nn.Linear(4096, 4096),
            nn.ReLU(inplace=True),
            nn.Dropout(0.5),
            nn.Linear(4096, num_classes),
        )
    
    def forward(self, x):
        x = self.features(x)
        x = x.view(x.size(0), -1)
        x = self.classifier(x)
        return x

# 使用之前定义的ResNet和DenseNet（适配CIFAR）

def get_model(name, num_classes=10):
    if name == 'LeNet':
        return LeNet5_CIFAR(num_classes)
    elif name == 'AlexNet':
        return AlexNet_CIFAR(num_classes)
    elif name == 'VGG16':
        return VGG16_CIFAR(num_classes)
    elif name == 'ResNet18':
        # ResNet18需要调整初始卷积
        from resnet import ResNet18
        model = ResNet18(num_classes)
        model.conv1 = nn.Conv2d(3, 64, kernel_size=3, stride=1, padding=1, bias=False)
        model.maxpool = nn.Identity()
        return model
    elif name == 'DenseNet121':
        # DenseNet需要调整初始卷积
        from densenet import densenet121
        model = densenet121()
        model.conv1 = nn.Conv2d(3, 64, kernel_size=3, stride=1, padding=1, bias=False)
        model.maxpool = nn.Identity()
        model.fc = nn.Linear(model.fc.in_features, num_classes)
        return model
    else:
        raise ValueError(f"Unknown model: {name}")

# 3. 训练函数

def train_model(model, train_loader, val_loader, epochs=200):
    device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
    model = model.to(device)
    
    optimizer = optim.SGD(model.parameters(), lr=0.1, 
                         momentum=0.9, weight_decay=1e-4)
    scheduler = optim.lr_scheduler.CosineAnnealingLR(optimizer, T_max=200)
    criterion = nn.CrossEntropyLoss()
    
    history = {'train_loss': [], 'train_acc': [], 'val_loss': [], 'val_acc': []}
    best_acc = 0
    
    for epoch in range(epochs):
        # 训练
        model.train()
        train_loss = 0
        correct = 0
        total = 0
        for inputs, targets in train_loader:
            inputs, targets = inputs.to(device), targets.to(device)
            optimizer.zero_grad()
            outputs = model(inputs)
            loss = criterion(outputs, targets)
            loss.backward()
            optimizer.step()
            
            train_loss += loss.item()
            _, predicted = outputs.max(1)
            total += targets.size(0)
            correct += predicted.eq(targets).sum().item()
        
        # 验证
        model.eval()
        val_loss = 0
        val_correct = 0
        val_total = 0
        with torch.no_grad():
            for inputs, targets in val_loader:
                inputs, targets = inputs.to(device), targets.to(device)
                outputs = model(inputs)
                loss = criterion(outputs, targets)
                
                val_loss += loss.item()
                _, predicted = outputs.max(1)
                val_total += targets.size(0)
                val_correct += predicted.eq(targets).sum().item()
        
        train_acc = 100. * correct / total
        val_acc = 100. * val_correct / val_total
        
        history['train_loss'].append(train_loss/len(train_loader))
        history['train_acc'].append(train_acc)
        history['val_loss'].append(val_loss/len(val_loader))
        history['val_acc'].append(val_acc)
        
        print(f'Epoch {epoch+1:3d}: '
              f'Train Loss: {train_loss/len(train_loader):.3f} '
              f'Train Acc: {train_acc:.2f}% '
              f'Val Acc: {val_acc:.2f}%')
        
        scheduler.step()
        best_acc = max(best_acc, val_acc)
    
    return best_acc, history

# 4. 对比实验

models = ['LeNet', 'AlexNet', 'VGG16', 'ResNet18', 'DenseNet121']
results = {}
histories = {}

print("="*60)
print("CIFAR-10 架构对比实验")
print("="*60)

for name in models:
    print(f"\n训练 {name}...")
    print("-"*40)
    
    model = get_model(name)
    best_acc, history = train_model(model, train_loader, val_loader, epochs=200)
    results[name] = best_acc
    histories[name] = history

# 5. 结果可视化

print("\n" + "="*60)
print("最终结果对比")
print("="*60)
for name, acc in results.items():
    print(f"{name:15s}: {acc:.2f}%")

# 绘制对比图

plt.figure(figsize=(15, 10))

# 准确率对比

plt.subplot(2, 2, 1)
model_names = list(results.keys())
accs = list(results.values())
plt.bar(model_names, accs)
plt.title('CIFAR-10 准确率对比')
plt.ylabel('Accuracy (%)')
plt.xticks(rotation=45)

# 训练曲线对比

plt.subplot(2, 2, 2)
for name, history in histories.items():
    plt.plot(history['val_acc'], label=name, alpha=0.7)
plt.title('验证准确率曲线')
plt.xlabel('Epoch')
plt.ylabel('Accuracy (%)')
plt.legend()
plt.grid(True, alpha=0.3)

# Loss曲线

plt.subplot(2, 2, 3)
for name, history in histories.items():
    plt.plot(history['val_loss'], label=name, alpha=0.7)
plt.title('验证损失曲线')
plt.xlabel('Epoch')
plt.ylabel('Loss')
plt.legend()
plt.grid(True, alpha=0.3)

# 参数量对比

plt.subplot(2, 2, 4)
params = {
    'LeNet': 61706,
    'AlexNet': 61100352,
    'VGG16': 138357544,
    'ResNet18': 11689512,
    'DenseNet121': 7980000
}
param_list = [params[name] for name in model_names]
plt.bar(model_names, param_list)
plt.title('参数量对比')
plt.ylabel('Parameters')
plt.xticks(rotation=45)
plt.yscale('log')

plt.tight_layout()
plt.savefig('cifar10_comparison.png', dpi=300, bbox_inches='tight')
plt.show()

# 6. 生成报告

print("\n" + "="*60)
print("实验报告")
print("="*60)
print(f"{'模型':<15} {'准确率':<10} {'参数量':<12} {'特点'}")
print("-"*60)
for name in model_names:
    acc = results[name]
    param = params[name]
    if name == 'LeNet':
        desc = "基础CNN"
    elif name == 'AlexNet':
        desc = "ReLU/Dropout"
    elif name == 'VGG16':
        desc = "深度堆叠"
    elif name == 'ResNet18':
        desc = "残差连接"
    elif name == 'DenseNet121':
        desc = "特征复用"
    print(f"{name:<15} {acc:<10.2f} {param:<12,} {desc}")

print("\n结论:")
print("- ResNet18和DenseNet121在CIFAR-10上表现最佳")
print("- VGG16参数量过大但精度一般")
print("- LeNet适合简单任务，AlexNet是深度学习入门")
```

**预期结果**:
```
LeNet:     ~70%
AlexNet:   ~80%
VGG16:     ~85%
ResNet18:  ~88%
DenseNet121: ~89%
```

**项目产出**:
- [ ] 完整训练代码
- [ ] 对比图表（准确率、损失、参数量）
- [ ] 实验报告（分析结论）
- [ ] 模型权重文件

---

## 项目2: 架构修改实验

**目标**: 理解各组件的作用

**难度**: ⭐⭐⭐⭐ | **时间**: 2-3天

### 实验1: 残差连接的影响

```python

# 依赖: matplotlib, torch, torchvision
# 安装: pip install matplotlib torch torchvision
import torch
import torch.nn as nn
import torch.optim as optim
from torchvision import datasets, transforms
from torch.utils.data import DataLoader

# 创建对比数据集（简化版CIFAR-10）

transform = transforms.Compose([
    transforms.ToTensor(),
    transforms.Normalize((0.5,), (0.5,))
])

train_loader = DataLoader(
    datasets.CIFAR10('./data', train=True, download=True, transform=transform),
    batch_size=64, shuffle=True
)

# A: 普通网络（无残差）

class PlainNet(nn.Module):
    def __init__(self, depth=20):
        super().__init__()
        layers = []
        for _ in range(depth):
            layers.append(nn.Sequential(
                nn.Conv2d(3, 64, 3, padding=1),
                nn.BatchNorm2d(64),
                nn.ReLU()
            ))
        self.net = nn.Sequential(*layers)
        self.fc = nn.Linear(64, 10)
    
    def forward(self, x):
        x = self.net(x)
        x = x.mean(dim=[2,3])
        return self.fc(x)

# B: 残差网络

class ResidualNet(nn.Module):
    def __init__(self, depth=20):
        super().__init__()
        self.conv1 = nn.Conv2d(3, 64, 3, padding=1)
        self.blocks = nn.ModuleList([
            nn.Sequential(
                nn.Conv2d(64, 64, 3, padding=1),
                nn.BatchNorm2d(64),
                nn.ReLU()
            ) for _ in range(depth)
        ])
        self.fc = nn.Linear(64, 10)
    
    def forward(self, x):
        x = self.conv1(x)
        for block in self.blocks:
            x = x + block(x)  # 残差连接
        x = x.mean(dim=[2,3])
        return self.fc(x)

def train_network(model_class, name, epochs=50):
    device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
    model = model_class().to(device)
    optimizer = optim.SGD(model.parameters(), lr=0.1, momentum=0.9)
    criterion = nn.CrossEntropyLoss()
    
    losses = []
    for epoch in range(epochs):
        model.train()
        total_loss = 0
        for inputs, targets in train_loader:
            inputs, targets = inputs.to(device), targets.to(device)
            optimizer.zero_grad()
            outputs = model(inputs)
            loss = criterion(outputs, targets)
            loss.backward()
            optimizer.step()
            total_loss += loss.item()
        
        avg_loss = total_loss / len(train_loader)
        losses.append(avg_loss)
        if epoch % 10 == 0:
            print(f"{name} Epoch {epoch}: Loss = {avg_loss:.4f}")
    
    return losses

# 对比实验

print("实验1: 残差连接的影响 (20层网络)")
print("="*50)

plain_losses = train_network(PlainNet, "PlainNet", epochs=50)
residual_losses = train_network(ResidualNet, "ResidualNet", epochs=50)

# 可视化

import matplotlib.pyplot as plt
plt.figure(figsize=(10, 6))
plt.plot(plain_losses, label='PlainNet (无残差)', linewidth=2)
plt.plot(residual_losses, label='ResidualNet (有残差)', linewidth=2)
plt.title('残差连接对深度网络训练的影响')
plt.xlabel('Epoch')
plt.ylabel('Loss')
plt.legend()
plt.grid(True, alpha=0.3)
plt.savefig('residual_comparison.png', dpi=300)
plt.show()

print("\n结论:")
print("- PlainNet在深度增加时训练困难")
print("- ResidualNet保持稳定收敛")
print("- 残差连接有效解决梯度消失")
```

### 实验2: VGG通道数的影响

```python
def make_vgg(num_channels=64):
    """创建不同通道数的VGG"""
    return nn.Sequential(
        nn.Conv2d(3, num_channels, 3, padding=1),
        nn.ReLU(),
        nn.Conv2d(num_channels, num_channels, 3, padding=1),
        nn.ReLU(),
        nn.MaxPool2d(2),
        
        nn.Conv2d(num_channels, num_channels*2, 3, padding=1),
        nn.ReLU(),
        nn.Conv2d(num_channels*2, num_channels*2, 3, padding=1),
        nn.ReLU(),
        nn.MaxPool2d(2),
        
        nn.Conv2d(num_channels*2, num_channels*4, 3, padding=1),
        nn.ReLU(),
        nn.Conv2d(num_channels*4, num_channels*4, 3, padding=1),
        nn.ReLU(),
        nn.MaxPool2d(2),
        
        nn.Flatten(),
        nn.Linear(num_channels*4*4*4, 1024),
        nn.ReLU(),
        nn.Linear(1024, 10)
    )

# 对比不同通道数

for channels in [32, 64, 128]:
    model = make_vgg(channels)
    params = sum(p.numel() for p in model.parameters())
    print(f"通道数 {channels}: 参数量 = {params:,}")
```

### 实验3: DenseNet增长率的影响

```python
def test_growth_rate():
    """测试不同growth_rate的效果"""
    from densenet import DenseNet
    
    for gr in [12, 24, 32, 48]:
        model = DenseNet(growth_rate=gr, block_config=(4, 4, 4, 4))
        params = sum(p.numel() for p in model.parameters()) / 1e6
        print(f"growth_rate={gr}: {params:.1f}M 参数")
```

**项目产出**:
- [ ] 残差连接对比实验代码
- [ ] 通道数影响分析
- [ ] 增长率影响分析
- [ ] 实验结论报告

---

## 项目3: 迁移学习实战

**目标**: 使用预训练模型解决新任务

**难度**: ⭐⭐⭐⭐ | **时间**: 3-4天

### 完整迁移学习流程

```python

# 依赖: PIL, torchvision
# 安装: pip install PIL torchvision
import torchvision.models as models
from torch.utils.data import Dataset
import os
from PIL import Image

# 1. 准备自定义数据集

class CustomDataset(Dataset):
    def __init__(self, root_dir, transform=None):
        self.root_dir = root_dir
        self.transform = transform
        self.classes = sorted(os.listdir(root_dir))
        self.class_to_idx = {cls: idx for idx, cls in enumerate(self.classes)}
        self.images = []
        self.labels = []
        
        for cls in self.classes:
            cls_dir = os.path.join(root_dir, cls)
            for img_name in os.listdir(cls_dir):
                self.images.append(os.path.join(cls_dir, img_name))
                self.labels.append(self.class_to_idx[cls])
    
    def __len__(self):
        return len(self.images)
    
    def __getitem__(self, idx):
        img = Image.open(self.images[idx]).convert('RGB')
        label = self.labels[idx]
        if self.transform:
            img = self.transform(img)
        return img, label

# 2. 数据预处理

train_transform = transforms.Compose([
    transforms.RandomResizedCrop(224),
    transforms.RandomHorizontalFlip(),
    transforms.ColorJitter(brightness=0.2, contrast=0.2),
    transforms.ToTensor(),
    transforms.Normalize([0.485, 0.456, 0.406], [0.229, 0.224, 0.225])
])

val_transform = transforms.Compose([
    transforms.Resize(256),
    transforms.CenterCrop(224),
    transforms.ToTensor(),
    transforms.Normalize([0.485, 0.456, 0.406], [0.229, 0.224, 0.225])
])

# 3. 加载预训练模型

def load_pretrained_model(num_classes, architecture='resnet50'):
    """
    加载预训练模型并修改分类头
    
    Args:
        num_classes: 新任务的类别数
        architecture: 'resnet50', 'densenet121', 'vgg16'等
    """
    if architecture == 'resnet50':
        model = models.resnet50(pretrained=True)
        num_features = model.fc.in_features
        model.fc = nn.Linear(num_features, num_classes)
        
    elif architecture == 'densenet121':
        model = models.densenet121(pretrained=True)
        num_features = model.classifier.in_features
        model.classifier = nn.Linear(num_features, num_classes)
        
    elif architecture == 'vgg16':
        model = models.vgg16(pretrained=True)
        num_features = model.classifier[6].in_features
        model.classifier[6] = nn.Linear(num_features, num_classes)
    
    return model

# 4. 冻结策略

def freeze_layers(model, architecture='resnet50'):
    """冻结部分层"""
    if architecture == 'resnet50':
        # 冻结所有层
        for param in model.parameters():
            param.requires_grad = False
        
        # 只解冻最后两层
        for param in model.layer4.parameters():
            param.requires_grad = True
        for param in model.fc.parameters():
            param.requires_grad = True
        
    elif architecture == 'densenet121':
        # 冻结所有层
        for param in model.parameters():
            param.requires_grad = False
        
        # 解冻最后几个denseblock
        for param in model.features.denseblock4.parameters():
            param.requires_grad = True
        for param in model.classifier.parameters():
            param.requires_grad = True
    
    return model

# 5. 分层学习率

def get_optimizer(model, architecture='resnet50'):
    """不同层使用不同学习率"""
    if architecture == 'resnet50':
        optimizer = torch.optim.Adam([
            {'params': model.fc.parameters(), 'lr': 0.001},
            {'params': model.layer4.parameters(), 'lr': 0.0001},
            {'params': model.layer3.parameters(), 'lr': 0.0001},
            {'params': model.layer2.parameters(), 'lr': 0.0001},
            {'params': model.layer1.parameters(), 'lr': 0.0001},
            {'params': model.conv1.parameters(), 'lr': 0.0001},
        ], lr=0.001)
    else:
        optimizer = torch.optim.Adam(model.parameters(), lr=0.001)
    
    return optimizer

# 6. 训练策略

def train_transfer_learning(model, train_loader, val_loader, epochs=20):
    device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
    model = model.to(device)
    
    criterion = nn.CrossEntropyLoss()
    optimizer = get_optimizer(model)
    scheduler = torch.optim.lr_scheduler.ReduceLROnPlateau(
        optimizer, mode='max', factor=0.5, patience=3
    )
    
    best_acc = 0
    history = {'train_loss': [], 'val_acc': []}
    
    for epoch in range(epochs):
        # 训练阶段
        model.train()
        train_loss = 0
        for inputs, labels in train_loader:
            inputs, labels = inputs.to(device), labels.to(device)
            optimizer.zero_grad()
            outputs = model(inputs)
            loss = criterion(outputs, labels)
            loss.backward()
            optimizer.step()
            train_loss += loss.item()
        
        # 验证阶段
        model.eval()
        val_correct = 0
        val_total = 0
        with torch.no_grad():
            for inputs, labels in val_loader:
                inputs, labels = inputs.to(device), labels.to(device)
                outputs = model(inputs)
                _, predicted = torch.max(outputs, 1)
                val_total += labels.size(0)
                val_correct += (predicted == labels).sum().item()
        
        train_loss_avg = train_loss / len(train_loader)
        val_acc = 100. * val_correct / val_total
        
        history['train_loss'].append(train_loss_avg)
        history['val_acc'].append(val_acc)
        
        print(f'Epoch {epoch+1:2d}: Train Loss = {train_loss_avg:.4f}, Val Acc = {val_acc:.2f}%')
        
        scheduler.step(val_acc)
        
        if val_acc > best_acc:
            best_acc = val_acc
            torch.save(model.state_dict(), 'best_model.pth')
    
    return best_acc, history

# 7. 完整使用示例

def complete_transfer_learning_example():
    """完整迁移学习示例"""
    
    # 假设你有自定义数据集
    # train_dataset = CustomDataset('data/train', transform=train_transform)
    # val_dataset = CustomDataset('data/val', transform=val_transform)
    
    # 这里用CIFAR-10演示
    train_dataset = datasets.CIFAR10('./data', train=True, 
                                     transform=train_transform, download=True)
    val_dataset = datasets.CIFAR10('./data', train=False, 
                                   transform=val_transform, download=True)
    
    train_loader = DataLoader(train_dataset, batch_size=32, shuffle=True)
    val_loader = DataLoader(val_dataset, batch_size=32, shuffle=False)
    
    # 策略1: 只训练分类头（小数据集）
    print("\n策略1: 只训练分类头")
    model = load_pretrained_model(num_classes=10, architecture='resnet50')
    model = freeze_layers(model, architecture='resnet50')
    best_acc, history1 = train_transfer_learning(model, train_loader, val_loader, epochs=10)
    print(f"最佳准确率: {best_acc:.2f}%")
    
    # 策略2: 微调所有层（大数据集）
    print("\n策略2: 微调所有层")
    model = load_pretrained_model(num_classes=10, architecture='resnet50')
    # 不冻结，全部微调
    best_acc, history2 = train_transfer_learning(model, train_loader, val_loader, epochs=20)
    print(f"最佳准确率: {best_acc:.2f}%")
    
    # 策略3: 分层微调（推荐）
    print("\n策略3: 分层微调")
    model = load_pretrained_model(num_classes=10, architecture='resnet50')
    model = freeze_layers(model, architecture='resnet50')
    best_acc, history3 = train_transfer_learning(model, train_loader, val_loader, epochs=20)
    print(f"最佳准确率: {best_acc:.2f}%")
    
    # 可视化对比
    plt.figure(figsize=(12, 5))
    
    plt.subplot(1, 2, 1)
    plt.plot(history1['train_loss'], label='只训练分类头')
    plt.plot(history2['train_loss'], label='微调所有层')
    plt.plot(history3['train_loss'], label='分层微调')
    plt.title('训练损失对比')
    plt.xlabel('Epoch')
    plt.ylabel('Loss')
    plt.legend()
    plt.grid(True, alpha=0.3)
    
    plt.subplot(1, 2, 2)
    plt.plot(history1['val_acc'], label='只训练分类头')
    plt.plot(history2['val_acc'], label='微调所有层')
    plt.plot(history3['val_acc'], label='分层微调')
    plt.title('验证准确率对比')
    plt.xlabel('Epoch')
    plt.ylabel('Accuracy (%)')
    plt.legend()
    plt.grid(True, alpha=0.3)
    
    plt.tight_layout()
    plt.savefig('transfer_learning_comparison.png', dpi=300)
    plt.show()

# 运行示例

if __name__ == "__main__":
    complete_transfer_learning_example()
```

**策略对比**:

| 策略 | 适用场景 | 优点 | 缺点 |
|------|----------|------|------|
| 只训练分类头 | 小数据集 (<1k) | 快速，不易过拟合 | 精度有限 |
| 微调所有层 | 大数据集 (>10k) | 精度高 | 需要更多数据 |
| 分层微调 | 中等数据集 | 平衡速度和精度 | 需要调参 |

**项目产出**:
- [ ] 三种策略的完整代码
- [ ] 性能对比图表
- [ ] 策略选择建议
- [ ] 实际应用案例

---

## 项目4: 模型性能分析

**目标**: 深入理解各架构的性能特点

**难度**: ⭐⭐⭐ | **时间**: 2天

### 分析代码

```python

# 依赖: time, torch, torchsummary
# 安装: pip install time torch torchsummary
import time
import torch
from torchsummary import summary

def analyze_model(model, input_size=(3, 224, 224), device='cuda'):
    """全面分析模型性能"""
    
    model = model.to(device)
    
    # 1. 参数量分析
    total_params = sum(p.numel() for p in model.parameters())
    trainable_params = sum(p.numel() for p in model.parameters() if p.requires_grad)
    
    print("="*60)
    print("模型参数分析")
    print("="*60)
    print(f"总参数量: {total_params:,} ({total_params/1e6:.2f}M)")
    print(f"可训练参数: {trainable_params:,} ({trainable_params/1e6:.2f}M)")
    print(f"参数占比: {trainable_params/total_params*100:.1f}%")
    
    # 2. 各层参数分布
    print("\n各层参数量:")
    for name, param in model.named_parameters():
        if param.requires_grad:
            print(f"  {name:40s}: {param.numel():>10,}")
    
    # 3. 计算量分析 (FLOPs估算)
    def count_flops(model, input_size):
        """估算FLOPs"""
        flops = 0
        x = torch.randn(1, *input_size).to(device)
        
        for module in model.modules():
            if isinstance(module, nn.Conv2d):
                # Conv2d FLOPs = 2 * C_in * C_out * H * W * K_h * K_w
                out_h = (x.shape[2] + 2*module.padding[0] - module.kernel_size[0]) // module.stride[0] + 1
                out_w = (x.shape[3] + 2*module.padding[1] - module.kernel_size[1]) // module.stride[1] + 1
                flops += 2 * x.shape[1] * module.out_channels * out_h * out_w * module.kernel_size[0] * module.kernel_size[1]
                x = module(x)
            elif isinstance(module, nn.Linear):
                # Linear FLOPs = 2 * C_in * C_out
                flops += 2 * x.shape[1] * module.out_features
                x = module(x)
            else:
                try:
                    x = module(x)
                except:
                    pass
        
        return flops
    
    flops = count_flops(model, input_size)
    print(f"\n估算FLOPs: {flops:,} ({flops/1e9:.2f}G)")
    
    # 4. 推理速度测试
    print("\n推理速度测试:")
    model.eval()
    batch_sizes = [1, 8, 32]
    
    with torch.no_grad():
        for bs in batch_sizes:
            x = torch.randn(bs, *input_size).to(device)
            
            # 预热
            for _ in range(10):
                _ = model(x)
            
            # 计时
            torch.cuda.synchronize()
            start = time.time()
            for _ in range(100):
                _ = model(x)
            torch.cuda.synchronize()
            end = time.time()
            
            avg_time = (end - start) / 100
            fps = bs / avg_time
            
            print(f"  Batch {bs:2d}: {avg_time*1000:6.2f}ms/batch, {fps:8.1f} FPS")
    
    # 5. 内存占用
    if device == 'cuda':
        memory_allocated = torch.cuda.memory_allocated() / 1024**2
        memory_reserved = torch.cuda.memory_reserved() / 1024**2
        print(f"\n显存占用:")
        print(f"  已分配: {memory_allocated:.2f} MB")
        print(f"  保留: {memory_reserved:.2f} MB")
    
    # 6. 模型结构摘要
    print("\n模型结构:")
    summary(model, input_size)

# 使用示例

if __name__ == "__main__":
    models_to_analyze = {
        'ResNet50': ResNet50(),
        'DenseNet121': densenet121(),
    }
    
    for name, model in models_to_analyze.items():
        print(f"\n{'='*60}")
        print(f"分析: {name}")
        print(f"{'='*60}")
        analyze_model(model)
        print("\n")
```

**项目产出**:
- [ ] 各架构性能分析报告
- [ ] 参数量对比表
- [ ] 计算量对比表
- [ ] 推理速度对比表

---

## 项目5: 模型压缩与优化

**目标**: 学习模型优化技术

**难度**: ⭐⭐⭐⭐⭐ | **时间**: 3-4天

### 1. 模型量化

```python
def quantize_model(model, val_loader):
    """动态量化"""
    import torch.quantization as quantization
    
    # 1. 准备模型
    model.eval()
    model.qconfig = quantization.get_default_qconfig('fbgemm')
    
    # 2. 融合模型（Conv+BN+ReLU）
    model.fuse_model()
    
    # 3. 校准
    def calibrate(model, val_loader):
        with torch.no_grad():
            for inputs, _ in val_loader:
                model(inputs)
    
    calibrate(model, val_loader)
    
    # 4. 量化
    quantized_model = quantization.quantize_dynamic(
        model, {nn.Linear, nn.Conv2d}, dtype=torch.qint8
    )
    
    return quantized_model

# 对比

def compare_quantization(model, val_loader):
    """对比量化前后"""
    device = next(model.parameters()).device
    
    # 原始模型
    model.eval()
    start = time.time()
    with torch.no_grad():
        for inputs, _ in val_loader:
            _ = model(inputs)
    original_time = time.time() - start
    original_size = sum(p.numel() for p in model.parameters())
    
    # 量化模型
    quantized = quantize_model(model, val_loader)
    start = time.time()
    with torch.no_grad():
        for inputs, _ in val_loader:
            _ = quantized(inputs)
    quantized_time = time.time() - start
    quantized_size = sum(p.numel() for p in quantized.parameters())
    
    print(f"原始模型: {original_time:.3f}s, {original_size/1e6:.1f}M参数")
    print(f"量化模型: {quantized_time:.3f}s, {quantized_size/1e6:.1f}M参数")
    print(f"加速: {original_time/quantized_time:.2f}x")
    print(f"压缩: {original_size/quantized_size:.2f}x")
```

### 2. 知识蒸馏

```python
class DistillationLoss(nn.Module):
    """知识蒸馏损失"""
    def __init__(self, T=3.0, alpha=0.7):
        super().__init__()
        self.T = T
        self.alpha = alpha
        self.kl_div = nn.KLDivLoss(reduction='batchmean')
        self.ce_loss = nn.CrossEntropyLoss()
    
    def forward(self, student_outputs, teacher_outputs, labels):
        # 软标签损失
        soft_loss = self.kl_div(
            F.log_softmax(student_outputs / self.T, dim=1),
            F.softmax(teacher_outputs / self.T, dim=1)
        ) * (self.T * self.T)
        
        # 硬标签损失
        hard_loss = self.ce_loss(student_outputs, labels)
        
        return self.alpha * soft_loss + (1 - self.alpha) * hard_loss

def train_distillation(teacher_model, student_model, train_loader, val_loader, epochs=50):
    """知识蒸馏训练"""
    device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
    teacher_model = teacher_model.to(device).eval()
    student_model = student_model.to(device)
    
    criterion = DistillationLoss(T=3.0, alpha=0.7)
    optimizer = optim.Adam(student_model.parameters(), lr=0.001)
    
    for epoch in range(epochs):
        student_model.train()
        for inputs, labels in train_loader:
            inputs, labels = inputs.to(device), labels.to(device)
            
            with torch.no_grad():
                teacher_outputs = teacher_model(inputs)
            
            student_outputs = student_model(inputs)
            loss = criterion(student_outputs, teacher_outputs, labels)
            
            optimizer.zero_grad()
            loss.backward()
            optimizer.step()
        
        # 验证
        student_model.eval()
        correct = 0
        total = 0
        with torch.no_grad():
            for inputs, labels in val_loader:
                inputs, labels = inputs.to(device), labels.to(device)
                outputs = student_model(inputs)
                _, predicted = torch.max(outputs, 1)
                total += labels.size(0)
                correct += (predicted == labels).sum().item()
        
        print(f"Epoch {epoch+1}: Val Acc = {100.*correct/total:.2f}%")
    
    return student_model
```

**项目产出**:
- [ ] 量化前后对比代码
- [ ] 知识蒸馏实现
- [ ] 压缩效果分析
- [ ] 部署建议

---

## 📊 项目成果展示

### 1. 实验报告模板

```bash
# 经典CNN架构对比实验报告

## 实验目标

- 复现5种经典CNN架构
- 在CIFAR-10上进行对比实验
- 分析各架构的性能特点

## 实验设置

- 数据集: CIFAR-10
- 输入尺寸: 32×32
- 优化器: SGD (lr=0.1, momentum=0.9)
- 训练轮数: 200轮
- 数据增强: 随机裁剪+翻转

## 实验结果

| 模型 | 准确率 | 参数量 | 训练时间 | 特点 |
|------|--------|--------|----------|------|
| LeNet-5 | 70.2% | 61K | 0.5h | 基础CNN |
| AlexNet | 80.5% | 61M | 2h | ReLU/Dropout |
| VGG16 | 85.3% | 138M | 5h | 深度堆叠 |
| ResNet18 | 88.1% | 11.7M | 1.5h | 残差连接 |
| DenseNet121 | 89.2% | 8.0M | 2h | 特征复用 |

## 结论分析

1. ResNet18在参数量和准确率上最佳平衡
2. DenseNet121参数效率最高
3. VGG16参数量过大，不适合实际应用
4. LeNet适合教学和简单任务
5. AlexNet是深度学习入门经典

## 改进建议

- 尝试不同学习率调度
- 增加数据增强策略
- 使用预训练模型
- 尝试模型融合
```

### 2. 可视化展示

```python
def create_comparison_dashboard():
    """创建对比仪表板"""
    fig, axes = plt.subplots(2, 3, figsize=(18, 12))
    
    # 1. 准确率对比
    axes[0,0].bar(models, accuracies)
    axes[0,0].set_title('准确率对比')
    axes[0,0].set_ylabel('Accuracy (%)')
    
    # 2. 参数量对比
    axes[0,1].bar(models, params)
    axes[0,1].set_title('参数量对比')
    axes[0,1].set_ylabel('Parameters (M)')
    axes[0,1].set_yscale('log')
    
    # 3. 计算量对比
    axes[0,2].bar(models, flops)
    axes[0,2].set_title('计算量对比')
    axes[0,2].set_ylabel('FLOPs (G)')
    
    # 4. 性能-效率散点图
    axes[1,0].scatter(params, accuracies)
    for i, model in enumerate(models):
        axes[1,0].annotate(model, (params[i], accuracies[i]))
    axes[1,0].set_xlabel('Parameters (M)')
    axes[1,0].set_ylabel('Accuracy (%)')
    axes[1,0].set_title('性能-效率权衡')
    axes[1,0].grid(True, alpha=0.3)
    
    # 5. 训练曲线
    for model in models:
        axes[1,1].plot(histories[model]['val_acc'], label=model)
    axes[1,1].set_title('验证准确率曲线')
    axes[1,1].set_xlabel('Epoch')
    axes[1,1].set_ylabel('Accuracy (%)')
    axes[1,1].legend()
    axes[1,1].grid(True, alpha=0.3)
    
    # 6. 损失曲线
    for model in models:
        axes[1,2].plot(histories[model]['val_loss'], label=model)
    axes[1,2].set_title('验证损失曲线')
    axes[1,2].set_xlabel('Epoch')
    axes[1,2].set_ylabel('Loss')
    axes[1,2].legend()
    axes[1,2].grid(True, alpha=0.3)
    
    plt.tight_layout()
    plt.savefig('dashboard.png', dpi=300, bbox_inches='tight')
    plt.show()
```

---

## 🎯 项目进阶建议

### 1. 扩展实验

- [ ] 在ImageNet子集上测试
- [ ] 尝试不同数据集（CIFAR-100, SVHN）
- [ ] 对比不同优化器（Adam vs SGD）
- [ ] 测试不同学习率调度策略

### 2. 工程化

- [ ] 使用TensorBoard记录实验
- [ ] 实现模型保存和加载
- [ ] 添加断点续训功能
- [ ] 实现多GPU训练

### 3. 部署相关

- [ ] 导出ONNX格式
- [ ] 使用TensorRT加速
- [ ] 模型量化（INT8/FP16）
- [ ] 移动端部署

### 4. 研究创新

- [ ] 设计新的残差块
- [ ] 尝试混合架构
- [ ] 改进注意力机制
- [ ] 自动化架构搜索

---

**文档版本**: v1.1  
**最后更新**: 2026-12-23  
**难度等级**: ⭐⭐⭐⭐  
**建议完成时间**: 2-4周
