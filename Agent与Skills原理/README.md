# Agent 与 Skills 原理

这个文件夹用三篇文档讲清楚当下 AI 编程助手（TRAE、Claude Code、Cursor、Codex 等）背后的两块核心机械：**Agent（智能体）** 和 **Skills（技能）**，并附一个真实开源项目的实战拆解。

- **Agent 回答的问题是**：怎么让一个"只会生成文本"的语言模型，真正去*做事*——读代码、改文件、跑命令？
- **Skills 回答的问题是**：领域知识和最佳实践那么多，上下文窗口塞不下，怎么*按需加载*给模型？
- **实战篇回答的问题是**：给一个专业领域，怎么从 0 到 1 设计出一整套生产级 skill？

一句话概括二者关系：

> **Agent 是发动机（模型 + 工具 + 循环），Skills 是随车手册（按需加载的专家知识包）。**

## 这套文档怎么写的（教学设计说明）

写法上参考了三个公认的优秀课程，并在中文语境下重新组织：

| 参考对象 | 借鉴的手法 |
| --- | --- |
| 微软《AI Agents for Beginners》（18 课） | **贯穿全文的案例**（我们用"程序员和 AI 助手小助"）、**反例教学**（四种上下文事故）、**行为动词自测清单** |
| Hugging Face《Agents Course》 | **故事先行、定义后置**（先看小助翻车再给定义）、**能力阶梯**（最小代码差分辨析概念）、**先手搓再讲框架**（40 行代码写 Agent） |
| Datawhale《hello-agents》（中文开源） | **"会用轮子也会造轮子"的三层递进**：原理 → 手写 → 再看工具产品 |

两个特点：

1. **零基础友好**：每章都有"人话版"比喻块（玻璃房顾问、白板、目录卡片、点菜系统），跳过代码和表格也能读通；
2. **专业深度**：正文包含真实 API 报文、实测数字（58 个工具 ≈ 55K token、多智能体 15× 消耗、压缩前后 335K→169K）、可跟着敲的代码，以及全部一手来源链接。

## 总览图

```mermaid
flowchart TD
    U([用户消息]) --> CTX

    subgraph LOOP [Agent 主循环（harness 驱动，每轮一次模型推理）]
        CTX["上下文窗口 · 工作记忆（小助的白板）<br/>系统提示 / 工具定义 / 对话历史 / 工具结果"]
        CTX --> LLM[LLM 推理]
        LLM -- 普通文本 --> OUT([最终答复 · 循环结束])
        LLM -- "工具调用请求 tool_use" --> H["Harness 执行<br/>Read / Edit / Bash / 子代理 …"]
        H -- "结果回填 tool_result" --> CTX
    end

    SK["Skills · 按需加载的知识包<br/>name + description 常驻索引（~100 token/个）<br/>触发后才注入 SKILL.md 正文与脚本"]
    SK -. "渐进式披露" .-> CTX
    MCP["MCP Server · 标准化外部连接<br/>GitHub / Postgres / 浏览器 …"] -. "tools/list → tools/call" .-> H
```

## 阅读路径

| 文件 | 结构 | 适合谁 |
| --- | --- | --- |
| [01-Agent原理.md](01-Agent原理.md) | LLM 三个底层设定 → 循环解剖 → 工具调用（含裸 prompt 版）→ 上下文工程 → 编排与多智能体 → **40 行代码手写 Agent + 练习场** → 记忆/安全 → 自测清单 | 想理解 AI 编程助手"为什么能干活"的人 |
| [02-Skills原理.md](02-Skills原理.md) | 周报故事开场 → SKILL.md 规范 → 渐进式披露（带数字的完整时序）→ 触发机制与排障 → **15 分钟手写一个技能（含调试迭代）** → 自测清单 | 想给自己或团队写 skill 的人 |
| [03-实战解析-qa-skills设计.md](03-实战解析-qa-skills设计.md) | **跟着做教程**：10 分钟跑通第一个技能 → 踩坑（编造/格式漂移/膨胀）→ 重构拆层 → 长成 6 目录套件（触发竞争/薄编排/core 共享库/一票否决/评测）→ 每步对照 qa-skills 真实设计 | 想为自己的专业领域设计整套 skill 框架的人 |

建议先读 Agent 原理——Skills 本质上是给 Agent 这台机器设计的"供弹系统"，不懂发动机就很难理解为什么手册要那样设计。三篇共用同一位主角"小助"，情节连续：学会干活（01）→ 学会翻手册（02）→ 入职当测试工程师，看真实工程怎么把理论造出来（03）。

## 一张表看懂六个易混概念

| 概念 | 本质 | 解决什么 | 类比 |
| --- | --- | --- | --- |
| **Agent** | 模型 + 工具 + 循环的运行时 | 让模型能*做事* | 一整个工人 |
| **Tool（工具）** | harness 内置的可执行函数 | 给模型*能力* | 工人的手 |
| **Skill（技能）** | 一个文件夹 + SKILL.md | 给模型*知识和流程*（按需加载） | 操作手册（SOP） |
| **MCP** | 标准化的外部服务接入协议 | 让第三方系统被统一接入 | 外包协作接口 / USB-C |
| **子代理** | 独立上下文的分身 agent | 隔离脏活、并行探索 | 派出去的助手 |
| **AGENTS.md / CLAUDE.md** | 常驻项目说明文件 | 全程生效的项目背景 | 贴在墙上的厂规 |

工具决定"能不能做"，Skill 决定"怎么做最好"，MCP 决定"连得上谁"，规则文件决定"这个项目的常识"。

## 关键时间线

| 时间 | 事件 |
| --- | --- |
| 2022-10 | ReAct 论文确立 Thought→Action→Observation 循环范式 |
| 2023 | OpenAI Function Calling / Anthropic Tool Use 成为原生 API 能力 |
| 2024-11 | Anthropic 开源 MCP（Model Context Protocol） |
| 2024-12 | 《Building Effective Agents》：简单可组合的模式胜过复杂框架 |
| 2025 | Coding Agent 走向生产；上下文工程、多智能体系统成型 |
| 2025-10 | Anthropic 发布 Agent Skills |
| 2025-12 | Skills 成为开放标准（agentskills.io） |
| 2026 | 各主流工具全面支持 Skills 与 MCP，生态收敛 |

> 文中数字与产品细节截至 2026 年中，该领域迭代极快；两篇文末均附一手来源链接，可顺藤摸瓜看最新版。历史版本备份在 `.backup/`。
